# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/ee/user/application_security/sast/#customizing-the-sast-settings
# Secret Detection customization: https://docs.gitlab.com/ee/user/application_security/secret_detection/#customizing-settings
# Dependency Scanning customization: https://docs.gitlab.com/ee/user/application_security/dependency_scanning/#customizing-the-dependency-scanning-settings
# Container Scanning customization: https://docs.gitlab.com/ee/user/application_security/container_scanning/#customizing-the-container-scanning-settings
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ee/ci/variables/#cicd-variable-precedence

stages:
- gitlab-test
- yaml-lint
- jinja2-lint
- ansible-roles-lint
- molecule-roles-test
- ansible-playbooks-lint
- molecule-playbooks-test

variables:
  DOCKER_NEXUS_ADDRESS: "nexus-manager.net3107.storages.labo-cbz.net:8082"
  DOCKER_IMAGE_DEBIAN_ANSIBLE: "$DOCKER_NEXUS_ADDRESS/gitlabcicd/debian-11-ansible:v3.0"
  DOCKER_IMAGE_DOCKER_ANSIBLE: "$DOCKER_NEXUS_ADDRESS/gitlabcicd/docker-ansible:v3.0"
  DOCKER_IMAGE_DEBIAN_JINJA: "$DOCKER_NEXUS_ADDRESS/tuxmartin/jinja2-lint:2022-05-24"
  PIPELINE_SCENARIO_NAME: "gitlabci"

###############################################################################
#################### actions
###############################################################################

# To start, we need to lint role's files with a YML vision
# Because if one of the files doesn't respect YML conventions,
# we dont need to test rest of the pipeline
.yaml_lint: &yaml_lint
  stage: yaml-lint
  image: $DOCKER_IMAGE_DEBIAN_ANSIBLE
  script:
    - yamllint -c ./.yamllint $path # Here mains files
  allow_failure: false
  retry: 2

# Then we need to lint on role's files with a JINJA2 vision
# Because if one of the templates files doesn't respect JINJA2 conventions,
# we dont need to test rest of the pipeline
.jinja2_lint: &jinja2_lint
  stage: jinja2-lint
  image:
    name: $DOCKER_IMAGE_DEBIAN_JINJA
    entrypoint: ["/bin/sh", "-c"]
  script:
    # Fix weird ansible bug: https://github.com/trailofbits/algo/issues/1637
    # This probably happens due to gitlab-runner mounting the git repo into the container
    - chmod o-w .
    - >
      find ./roles/*/templates/*.j2 \( -name '*' ! -iname ".*" \)
      -print0 | sort --zero-terminated | xargs -0 -I{} sh -c  '/usr/bin/python /j2lint.py {}'
  allow_failure: false
  retry: 2

.ansible_lint_role: &ansible_lint_role
  stage: ansible-roles-lint
  image: $DOCKER_IMAGE_DEBIAN_ANSIBLE
  script:
    - ansible-lint --config=./roles/$namespace.$role/.ansible-lint ./roles/$namespace.$role/
  allow_failure: false
  retry: 2

.molecule_test_role: &molecule_test_role
  stage: molecule-roles-test
  image:
    name: $DOCKER_IMAGE_DOCKER_ANSIBLE
  variables:
    PY_COLORS: '1'
    ANSIBLE_FORCE_COLOR: '1'
  #before_script:
    #- apk add --no-cache
    #  python3 python3-dev py3-pip gcc git curl build-base
    #  autoconf automake py3-cryptography linux-headers
    #  musl-dev libffi-dev openssl-dev openssh py3-yaml
    #- python3 -m pip install ansible molecule[docker]==4.0.4
    #- docker login -u "$NEXUS_REPOS_USERNAME" -p "$NEXUS_REPOS_PASSWORD" http://$DOCKER_NEXUS_ADDRESS
    #- docker info
    #- ansible-galaxy collection install community.general
    #- ansible-galaxy collection install community.docker
  script:
    - docker login -u "$NEXUS_REPOS_USERNAME" -p "$NEXUS_REPOS_PASSWORD" http://$DOCKER_NEXUS_ADDRESS
    - docker info
    - python3 -m compileall /usr/lib/python3 -qq
    - python3 --version
    - ansible --version
    - molecule --version
    # Fix weird ansible bug: https://github.com/trailofbits/algo/issues/1637
    # This probably happens due to gitlab-runner mounting the git repo into the container
    - cp ./.ansible.cfg /root/.ansible.cfg
    - cd ./roles/$namespace.$role
    - chmod o-w .
    - molecule test --scenario-name $PIPELINE_SCENARIO_NAME
  allow_failure: false
  retry: 2

.ansible_lint_playbook: &ansible_lint_playbook
  stage: ansible-playbooks-lint
  image: $DOCKER_IMAGE_DEBIAN_ANSIBLE
  script:
    - ansible-lint --config=./playbooks/$namespace.$playbook/.ansible-lint ./playbooks/$namespace.$playbook/
  allow_failure: false
  retry: 2

.molecule_test_playbook: &molecule_test_playbook
  stage: molecule-playbooks-test
  image:
    name: $DOCKER_IMAGE_DOCKER_ANSIBLE
  variables:
    PY_COLORS: '1'
    ANSIBLE_FORCE_COLOR: '1'
  #before_script:
    #- apk add --no-cache
    #  python3 python3-dev py3-pip gcc git curl build-base
    #  autoconf automake py3-cryptography linux-headers
    #  musl-dev libffi-dev openssl-dev openssh py3-yaml
    #- python3 -m pip install ansible molecule[docker]==4.0.4
    #- docker login -u "$NEXUS_REPOS_USERNAME" -p "$NEXUS_REPOS_PASSWORD" http://$DOCKER_NEXUS_ADDRESS
    #- docker info
    #- ansible-galaxy collection install community.general
    #- ansible-galaxy collection install community.docker
  script:
    - docker login -u "$NEXUS_REPOS_USERNAME" -p "$NEXUS_REPOS_PASSWORD" http://$DOCKER_NEXUS_ADDRESS
    - docker info
    - python3 -m compileall /usr/lib/python3 -qq
    - python3 --version
    - ansible --version
    - molecule --version
    # Fix weird ansible bug: https://github.com/trailofbits/algo/issues/1637
    # This probably happens due to gitlab-runner mounting the git repo into the container
    - cp ./.ansible.cfg /root/.ansible.cfg
    - cd ./playbooks/$namespace.$playbook
    - chmod o-w .
    - molecule test --scenario-name $PIPELINE_SCENARIO_NAME
  allow_failure: false
  retry: 2

###############################################################################
#################### CI JOBS
###############################################################################

############################################
#### gitlab-test
############################################

# As a GitLab ci, we use the default template :)
sast:
  stage: gitlab-test
include:
- template: Security/SAST.gitlab-ci.yml
  allow_failure: false

############################################
#### yaml-lint
############################################

yaml_lint_roles:
  <<: *yaml_lint
  variables:
    path: "./roles/*/*/*.yml"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

yaml_lint_playbooks:
  <<: *yaml_lint
  variables:
    path: "./playbooks/*/playbook.yml"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

yaml_lint_roles_molecule:
  <<: *yaml_lint
  variables:
    path: "./roles/*/molecule/*/*.yml"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

yaml_lint_playbooks_molecule:
  <<: *yaml_lint
  variables:
    path: "./playbooks/*/molecule/*/*.yml"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

yaml_lint_roles_inventories:
  <<: *yaml_lint
  variables:
    path: "./roles/*/tests/inventory/hosts.yml ./roles/*/tests/inventory/*/*.yml"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

yaml_lint_playbooks_inventories:
  <<: *yaml_lint
  variables:
    path: "./playbooks/*/tests/inventory/hosts.yml ./roles/*/tests/inventory/*/*.yml"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

#############################################
##### jinja2-lint
#############################################

jinja2_lint_roles:
  <<: *jinja2_lint
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### ansible-roles-lint && molecule
############################################

############################################
#### bootstrap_role
############################################

ansible_lint_role_bootstrap_role:
  <<: *ansible_lint_role
  variables:
    role: "bootstrap_role"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-bootstrap-role/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_bootstrap_role:
  <<: *molecule_test_role
  variables:
    role: "bootstrap_role"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-bootstrap-role/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_ssh
############################################

ansible_lint_role_install_ssh:
  <<: *ansible_lint_role
  variables:
    role: "install_ssh"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-ssh/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_ssh:
  <<: *molecule_test_role
  variables:
    role: "install_ssh"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-ssh/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_byobu
############################################

ansible_lint_role_install_byobu:
  <<: *ansible_lint_role
  variables:
    role: "install_byobu"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-byobu/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_byobu:
  <<: *molecule_test_role
  variables:
    role: "install_byobu"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-byobu/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_glances
############################################

ansible_lint_role_install_glances:
  <<: *ansible_lint_role
  variables:
    role: "install_glances"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-glances/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_glances:
  <<: *molecule_test_role
  variables:
    role: "install_glances"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-glances/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_fail2ban
############################################

ansible_lint_role_install_fail2ban:
  <<: *ansible_lint_role
  variables:
    role: "install_fail2ban"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-fail-2-ban/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_fail2ban:
  <<: *molecule_test_role
  variables:
    role: "install_fail2ban"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-fail-2-ban/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_docker
############################################

ansible_lint_role_install_docker:
  <<: *ansible_lint_role
  variables:
    role: "install_docker"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-docker/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_docker:
  <<: *molecule_test_role
  variables:
    role: "install_docker"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-docker/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_java
############################################

ansible_lint_role_install_java:
  <<: *ansible_lint_role
  variables:
    role: "install_java"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-java/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_java:
  <<: *molecule_test_role
  variables:
    role: "install_java"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-java/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### add_ldap_account_auth
############################################

ansible_lint_role_add_ldap_account_auth:
  <<: *ansible_lint_role
  variables:
    role: "add_ldap_account_auth"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-ldap-account-auth/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_add_ldap_account_auth:
  <<: *molecule_test_role
  variables:
    role: "add_ldap_account_auth"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-ldap-account-auth/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### add_logrotate_confs
############################################

ansible_lint_role_add_logrotate_confs:
  <<: *ansible_lint_role
  variables:
    role: "add_logrotate_confs"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-logrotate-confs/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_add_logrotate_confs:
  <<: *molecule_test_role
  variables:
    role: "add_logrotate_confs"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-logrotate-confs/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_logwatch
############################################

ansible_lint_role_install_logwatch:
  <<: *ansible_lint_role
  variables:
    role: "install_logwatch"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-logwatch/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_logwatch:
  <<: *molecule_test_role
  variables:
    role: "install_logwatch"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-logwatch/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_lynis
############################################

ansible_lint_role_install_lynis:
  <<: *ansible_lint_role
  variables:
    role: "install_lynis"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-lynis/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_lynis:
  <<: *molecule_test_role
  variables:
    role: "install_lynis"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-lynis/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_portsentry
############################################

ansible_lint_role_install_portsentry:
  <<: *ansible_lint_role
  variables:
    role: "install_portsentry"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-portsentry/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_portsentry:
  <<: *molecule_test_role
  variables:
    role: "install_portsentry"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-portsentry/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_postfix
############################################

ansible_lint_role_install_postfix:
  <<: *ansible_lint_role
  variables:
    role: "install_postfix"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-postfix/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_postfix:
  <<: *molecule_test_role
  variables:
    role: "install_postfix"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-postfix/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_rkhunter
############################################

ansible_lint_role_install_rkhunter:
  <<: *ansible_lint_role
  variables:
    role: "install_rkhunter"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-rkhunter/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_rkhunter:
  <<: *molecule_test_role
  variables:
    role: "install_rkhunter"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-rkhunter/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_rsyslog
############################################

ansible_lint_role_install_rsyslog:
  <<: *ansible_lint_role
  variables:
    role: "install_rsyslog"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-rsyslog/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_rsyslog:
  <<: *molecule_test_role
  variables:
    role: "install_rsyslog"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-rsyslog/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_unattended_upgrades
############################################

ansible_lint_role_install_unattended_upgrades:
  <<: *ansible_lint_role
  variables:
    role: "install_unattended_upgrades"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-unattended-upgrades/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_unattended_upgrades:
  <<: *molecule_test_role
  variables:
    role: "install_unattended_upgrades"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-unattended-upgrades/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### prepare_host
############################################

ansible_lint_role_prepare_host:
  <<: *ansible_lint_role
  variables:
    role: "prepare_host"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-prepare-host/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_prepare_host:
  <<: *molecule_test_role
  variables:
    role: "prepare_host"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-prepare-host/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_haproxy
############################################

ansible_lint_role_install_haproxy:
  <<: *ansible_lint_role
  variables:
    role: "install_haproxy"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-haproxy/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_haproxy:
  <<: *molecule_test_role
  variables:
    role: "install_haproxy"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-haproxy/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_apache
############################################

ansible_lint_role_install_apache:
  <<: *ansible_lint_role
  variables:
    role: "install_apache"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-apache/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_apache:
  <<: *molecule_test_role
  variables:
    role: "install_apache"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-apache/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### add_haproxy_http_confs
############################################

ansible_lint_role_add_haproxy_http_confs:
  <<: *ansible_lint_role
  variables:
    role: "add_haproxy_http_confs"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-haproxy-http-confs/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_add_haproxy_http_confs:
  <<: *molecule_test_role
  variables:
    role: "add_haproxy_http_confs"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-haproxy-http-confs/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### add_haproxy_bdd_confs
############################################

ansible_lint_role_add_haproxy_bdd_confs:
  <<: *ansible_lint_role
  variables:
    role: "add_haproxy_bdd_confs"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-haproxy-bdd-confs/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_add_haproxy_bdd_confs:
  <<: *molecule_test_role
  variables:
    role: "add_haproxy_http_confs"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-haproxy-bdd-confs/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### bootstrap_ssl_files
############################################

ansible_lint_role_bootstrap_ssl_files:
  <<: *ansible_lint_role
  variables:
    role: "bootstrap_ssl_files"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-bootstrap-ssl-files/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_bootstrap_ssl_files:
  <<: *molecule_test_role
  variables:
    role: "bootstrap_ssl_files"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-bootstrap-ssl-files/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### bootstrap_playbook
############################################

ansible_lint_role_bootstrap_playbook:
  <<: *ansible_lint_role
  variables:
    role: "bootstrap_playbook"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-bootstrap-playbook/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_bootstrap_playbook:
  <<: *molecule_test_role
  variables:
    role: "bootstrap_playbook"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-bootstrap-playbook/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_influxdb
############################################

ansible_lint_role_install_influxdb:
  <<: *ansible_lint_role
  variables:
    role: "install_influxdb"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-influxdb/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_influxdb:
  <<: *molecule_test_role
  variables:
    role: "install_influxdb"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-influxdb/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_redis
############################################

ansible_lint_role_install_redis:
  <<: *ansible_lint_role
  variables:
    role: "install_redis"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-redis/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_redis:
  <<: *molecule_test_role
  variables:
    role: "install_redis"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-redis/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### add_apache_confs
############################################

ansible_lint_role_add_apache_confs:
  <<: *ansible_lint_role
  variables:
    role: "add_apache_confs"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-apache-confs/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_add_apache_confs:
  <<: *molecule_test_role
  variables:
    role: "add_apache_confs"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-apache-confs/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_elasticsearch
############################################

ansible_lint_role_install_elasticsearch:
  <<: *ansible_lint_role
  variables:
    role: "install_elasticsearch"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-elasticsearch/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_elasticsearch:
  <<: *molecule_test_role
  variables:
    role: "install_elasticsearch"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-elasticsearch/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_kibana
############################################

ansible_lint_role_install_kibana:
  <<: *ansible_lint_role
  variables:
    role: "install_kibana"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-kibana/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_kibana:
  <<: *molecule_test_role
  variables:
    role: "install_kibana"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-kibana/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### add_certificates
############################################

ansible_lint_role_add_certificates:
  <<: *ansible_lint_role
  variables:
    role: "add_certificates"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-certificates/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_add_certificates:
  <<: *molecule_test_role
  variables:
    role: "add_certificates"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-add-certificates/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### install_logstash
############################################

ansible_lint_role_install_logstash:
  <<: *ansible_lint_role
  variables:
    role: "install_logstash"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-logstash/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_role_install_logstash:
  <<: *molecule_test_role
  variables:
    role: "install_logstash"
    namespace: "labocbz"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /role-install-logstash/
    - if: $CI_COMMIT_BRANCH =~ /playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### ansible-playbooks-lint && molecule
############################################

############################################
#### demo.deploy_demo_app
############################################

ansible_lint_playbook_deploy_demo_app:
  <<: *ansible_lint_playbook
  variables:
    playbook: "deploy_demo_app"
    namespace: "demo"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /playbook-deploy-demo-app/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_playbook_deploy_demo_app:
  <<: *molecule_test_playbook
  variables:
    playbook: "deploy_demo_app"
    namespace: "demo"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /playbook-deploy-demo-app/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### tools.bootstrap_playbook
############################################

ansible_lint_playbook_bootstrap_playbook:
  <<: *ansible_lint_playbook
  variables:
    playbook: "bootstrap_playbook"
    namespace: "tools"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /playbook-bootstrap-playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_playbook_bootstrap_playbook:
  <<: *molecule_test_playbook
  variables:
    playbook: "bootstrap_playbook"
    namespace: "tools"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /playbook-bootstrap-playbook/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### tools.bootstrap_role
############################################

ansible_lint_playbook_bootstrap_role:
  <<: *ansible_lint_playbook
  variables:
    playbook: "bootstrap_role"
    namespace: "tools"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /playbook-bootstrap-role/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_playbook_bootstrap_role:
  <<: *molecule_test_playbook
  variables:
    playbook: "bootstrap_role"
    namespace: "tools"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /playbook-bootstrap-role/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

############################################
#### tools.bootstrap_ssl_files
############################################

ansible_lint_playbook_bootstrap_ssl_files:
  <<: *ansible_lint_playbook
  variables:
    playbook: "bootstrap_ssl_files"
    namespace: "tools"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /playbook-bootstrap-ssl-files/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

molecule_test_playbook_bootstrap_ssl_files:
  <<: *molecule_test_playbook
  variables:
    playbook: "bootstrap_ssl_files"
    namespace: "tools"
  rules:
    - if: $CI_COMMIT_BRANCH =~ /playbook-bootstrap-ssl-files/
    - if: $CI_COMMIT_BRANCH =~ /improvement/
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"
